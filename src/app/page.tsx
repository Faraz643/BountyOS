"use client";
import { useEffect,useMemo,useState } from "react";
type Row={bounty:any;analysis:any};
export default function Home(){const [rows,setRows]=useState<Row[]>([]);const [user,setUser]=useState<any>(null);const [q,setQ]=useState("");const [min,setMin]=useState(0);const [loading,setLoading]=useState(true);const [msg,setMsg]=useState("");async function load(){setLoading(true);const r=await fetch(`/api/bounties?q=${encodeURIComponent(q)}&minReward=${min}`,{cache:"no-store"});const j=await r.json();setRows(j.data||[]);setLoading(false);if(j.error)setMsg(j.error)}useEffect(()=>{fetch("/api/me").then(r=>r.json()).then(j=>setUser(j.user));load()},[]);const ranked=useMemo(()=>rows.filter(x=>x.analysis.estimatedHours.likely<=50),[rows]);async function action(id:string,a:string){const r=await fetch("/api/bounties/action",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({bountyId:id,action:a})});const j=await r.json();setMsg(j.error||`${a} saved`)}return <main className="shell"><aside><div className="brand">Bounty<span>OS</span></div><nav>{[["/","Dashboard"],["/bounties","Bounties"],["/saved","Saved"],["/attempts","My Attempts"],["/earnings","Earnings"],["/skills","Skills"],["/settings","Settings"]].map(([href,label],i)=><a className={i===0?"active":""} href={href} key={href}>{label}</a>)}</nav><div className="phase">BOUNTYOS<br/><b>HUNTER → SOLVER → EARNER</b></div></aside><section className="content"><header><div><p className="eyebrow">AI BOUNTY OPERATING SYSTEM</p><h1>Best opportunities <em>for you.</em></h1><p className="muted">Expected successful earnings per hour—not a raw issue list.</p></div>{user?<span className="user">@{user.username}</span>:<a className="github" href="/api/auth/github">Sign in with GitHub</a>}</header><section className="stats"><Stat n={String(rows.length)} label="Live opportunities"/><Stat n={String(rows.filter(x=>x.analysis.opportunityScore>=70).length)} label="High opportunity"/><Stat n={String(rows.filter(x=>x.analysis.competition!=="high").length)} label="Not high competition"/></section><div className="toolbar"><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search technology, issue, repository…"/><select value={min} onChange={e=>setMin(Number(e.target.value))}><option value="0">Any reward</option><option value="50">$50+</option><option value="100">$100+</option><option value="500">$500+</option><option value="1000">$1,000+</option></select><button onClick={load}>Refresh</button></div>{msg&&<div className="notice">{msg}</div>}<div className="sectionHead"><div><p className="eyebrow">RANKED LIVE FEED</p><h2>🔥 Highest-value work</h2></div></div>{loading?<div className="empty">Discovering and ranking live GitHub opportunities…</div>:<div className="cards">{ranked.slice(0,20).map(({bounty:b,analysis:a})=><article className="card" key={b.id}><div className="cardTop"><div><span className="pill">{a.opportunityScore>=80?"HIGH OPPORTUNITY":"REVIEW"}</span><h3>{b.issue.title}</h3><p className="repo">{b.repository.owner}/{b.repository.name} · #{b.issue.number}</p></div><strong>{b.reward.amount?`$${b.reward.amount.toLocaleString()}`:"Reward unclear"}</strong></div><div className="chips">{b.labels.slice(0,5).map((x:string)=><span key={x}>{x}</span>)}</div><div className="metrics">{[["Difficulty",a.difficulty.replaceAll("_"," ")],["Effort",`${a.estimatedHours.minimum}–${a.estimatedHours.maximum}h`],["Success",`${a.successProbability}%`],["Acceptance",`${a.acceptanceProbability}%`],["Fit",`${a.technicalFit}%`],["Score",`${a.opportunityScore}/100`]].map(([l,v])=><div key={l}><span>{l}</span><b>{v}</b></div>)}</div><div className="why"><b>Why:</b> {a.reasons.join(" · ")}</div><div className="actions"><a href={b.sourceUrl} target="_blank">Issue ↗</a><button onClick={()=>action(b.id,"save")}>Save</button><button onClick={()=>action(b.id,"skip")}>Skip</button><button onClick={()=>action(b.id,"attempt")}>Start attempt</button></div></article>)}</div>}</section></main>}
function Stat({n,label}:{n:string,label:string}){return <div className="stat"><b>{n}</b><span>{label}</span></div>}
