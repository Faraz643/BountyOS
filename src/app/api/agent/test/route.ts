import {NextResponse} from "next/server";import {getCurrentUser} from "@/lib/auth";import {GitHubClient} from "@/integrations/github/client";import {db} from "@/lib/db";
export async function POST(req:Request){const u=await getCurrentUser();if(!u||!u.githubAccessToken)return NextResponse.json({error:"GitHub authorization required"},{status:401});const {pullRequestId,approved}=await req.json();if(approved!==true)return NextResponse.json({error:"Explicit approval required before tests run."},{status:400});const pr=await db.pullRequest.findUnique({where:{id:pullRequestId}});if(!pr||pr.userId!==u.id)return NextResponse.json({error:"PR not found"},{status:404});const gh=new GitHubClient(u.githubAccessToken);await gh.request(`/repos/${pr.githubOwner}/${pr.githubRepo}/actions/workflows/bountyos-sandbox.yml/dispatches`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({ref:pr.branch,inputs:{branch:pr.branch}})});return NextResponse.json({ok:true,message:"Sandbox test workflow dispatched.",branch:pr.branch});}
