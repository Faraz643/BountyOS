import { NextResponse } from "next/server";
import { db } from "@/lib/db";
import { getCurrentUser } from "@/lib/auth";
import { GitHubClient } from "@/integrations/github/client";
export async function POST(req:Request){const user=await getCurrentUser();if(!user)return NextResponse.json({error:"Unauthorized"},{status:401});const {runId,approved}=await req.json();if(approved!==true)return NextResponse.json({error:"Explicit approval is required before repository changes."},{status:400});const run=await db.agentRun.findUnique({where:{id:runId}});if(!run||run.userId!==user.id||run.status!=="completed")return NextResponse.json({error:"Completed solver run not found"},{status:404});const bounty=run.bountyId?await db.bounty.findUnique({where:{id:run.bountyId}}):null;if(!bounty||!user.githubAccessToken)return NextResponse.json({error:"Bounty or GitHub authorization missing"},{status:400});const output=run.output as any;const files=Array.isArray(output?.files)?output.files:[];if(!files.length)return NextResponse.json({error:"Solver produced no files"},{status:422});const gh=new GitHubClient(user.githubAccessToken);const meta=await gh.repo(bounty.owner,bounty.repository);const branch=`bountyos/${bounty.issueNumber}-${Date.now()}`;await gh.createBranch(bounty.owner,bounty.repository,branch,meta.default_branch);for(const f of files.slice(0,30)){if(!f?.path||typeof f.content!=="string"||f.path.includes("..")||f.path.startsWith("/"))continue;let sha:string|undefined;try{const current=await gh.contents(bounty.owner,bounty.repository,f.path,meta.default_branch);sha=current.sha}catch{}await gh.putFile(bounty.owner,bounty.repository,f.path,f.content,branch,`BountyOS: solve #${bounty.issueNumber}`,sha)}const pr=await gh.pr(bounty.owner,bounty.repository,branch,meta.default_branch,`BountyOS: ${bounty.title}`,`${output.summary||"AI-generated implementation"}\n\nGenerated with BountyOS. Please review all changes before merge.`);const record=await db.pullRequest.create({data:{userId:user.id,bountyId:bounty.id,number:pr.number,url:pr.html_url,githubOwner:bounty.owner,githubRepo:bounty.repository,branch,status:"open"}});return NextResponse.json({pullRequest:record});}
